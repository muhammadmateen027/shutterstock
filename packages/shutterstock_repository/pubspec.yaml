name: shutterstock_repository
description: Dart package which manages the reposity
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  network:
    path: ../network
  flutter_dotenv: ^5.0.0
  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.6
  json_serializable:
  mocktail: ^0.1.1
  very_good_analysis: ^2.1.0
